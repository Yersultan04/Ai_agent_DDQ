DATABASE_URL="file:./dev.db"

# Agent generation provider: local | openai
AGENT_PROVIDER="local"

# Optional OpenAI config (used only when AGENT_PROVIDER=openai)
OPENAI_API_KEY=""
OPENAI_MODEL="gpt-4o-mini"
